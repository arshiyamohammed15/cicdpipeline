# Gold Standards contracts (gold_standards)

This folder contains **OpenAPI**, **JSON Schemas**, and **example JSONs** for the module named **Gold Standards** (slug: `gold_standards`).

> Deterministic scaffold: replace placeholders with your real contracts. A placement report is included for auditing.
