# Detection Engine Core contracts (detection_engine_core)

This folder contains **OpenAPI**, **JSON Schemas**, and **example JSONs** for the module named **Detection Engine Core** (slug: `detection_engine_core`).

> Deterministic scaffold: replace placeholders with your real contracts. A placement report is included for auditing.
