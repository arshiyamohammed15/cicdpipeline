# Merge Conflicts & Delays contracts (merge_conflicts_and_delays)

This folder contains **OpenAPI**, **JSON Schemas**, and **example JSONs** for the module named **Merge Conflicts & Delays** (slug: `merge_conflicts_and_delays`).

> Deterministic scaffold: replace placeholders with your real contracts. A placement report is included for auditing.
