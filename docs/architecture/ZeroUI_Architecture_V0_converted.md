# ZeroUI Architecture V0 (Converted)

This document references OpenAPI and schema assets stored under the `contracts/` directory.

Notes:
- OpenAPI specs live in `contracts/` to keep API definitions centralized.
- Schema assets are also stored in `contracts/`.
