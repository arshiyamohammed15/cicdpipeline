- **PSCL command id (prepare plan)**: `zeroui.pscl.preparePlan` registered via `vscode.commands.registerCommand` in `src/vscode-extension/extension.ts`.
- **Pre-commit pipeline entry function**: `PreCommitValidationPipeline.run()` defined in `src/vscode-extension/shared/validation/PreCommitValidationPipeline.ts`.
- **Receipts JSONL path resolver & pattern**: `StoragePathResolver.resolveReceiptPath()` in `src/vscode-extension/shared/storage/StoragePathResolver.ts` returns `${ZU_ROOT}/ide/receipts/{repo-id}/{yyyy}/{mm}/`; `ReceiptStorageService`/`ReceiptStorageReader` append `receipts.jsonl`.
- **PSCL feature flag key**: `zeroui.pscl.enabled` declared under `contributes.configuration` in `src/vscode-extension/package.json`.
- **Policy snapshot reader/cache path**: `PolicySnapshotReader` in `src/vscode-extension/shared/storage/PolicySnapshotReader.ts` reads `${ZU_ROOT}/ide/policy/current/{policyId}.json` and `${ZU_ROOT}/ide/policy/cache/{policyId}-{version}.json`.
- **Trust-store public key directories**: `ReceiptVerifier` in `src/vscode-extension/shared/storage/ReceiptVerifier.ts` scans `${ZU_ROOT}/product/policy/trust/pubkeys` and `${ZU_ROOT}/tenant/policy/trust/pubkeys`.
- **Private-key loader module (signing path)**: `PlanExecutionAgent.loadPrivateKey()` in `src/vscode-extension/shared/storage/PlanExecutionAgent.ts` reads `${ZU_ROOT}/ide/trust/private/{keyId}.pem` (unless overridden).
- **Receipt signer module**: `Ed25519ReceiptSigner` in `src/vscode-extension/shared/storage/ReceiptSigner.ts`.
- **Receipt signature validator (current)**: `verifyReceiptSignature()` in `src/vscode-extension/shared/storage/ReceiptVerifier.ts`.
- **Receipt types / evaluation_point**: `DecisionReceipt` interface in `src/vscode-extension/shared/receipt-parser/ReceiptParser.ts` defines `inputs: Record<string, any>` (no explicit `evaluation_point` field; value supplied within `inputs` when generated by `PlanExecutionAgent`).
- **Plan artefact writer modules**: `PSCLArtifactWriter` in `src/vscode-extension/shared/storage/PSCLArtifactWriter.ts` emits `FileEnvelope.json` and `BuildPlan.json` into the PSCL temp directory.
- **Plan verifier & build sandbox modules**: `PlanVerifier` in `src/vscode-extension/shared/storage/PlanVerifier.ts` and `BuildSandbox` in `src/vscode-extension/shared/storage/BuildSandbox.ts`.
